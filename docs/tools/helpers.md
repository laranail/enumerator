# Helpers

Two utility classes in `src/Helpers/`:

- `Bitmask` — value object built by `HasBitmask::mask()` / `fromMask()`.
- `Humanizer` — pure string helpers used internally (`humanize`, `slugify`).
