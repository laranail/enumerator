# Notification channel example

Pattern for a rich domain enum using attributes + grouping. See `laranail/laranail`'s `NotificationChannelEnum` for a real-world implementation (15 channels grouped into core/messaging/mobile/integration buckets).

---

[← Docs index](../../README.md#documentation)
